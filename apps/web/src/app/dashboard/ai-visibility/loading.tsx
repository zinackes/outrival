// AI Visibility (often plan-gated → upsell). Neutral generic fallback, matching prior
// root-inherited behaviour; explicit so it doesn't inherit the overview-shaped root.
export { default } from "@/app/dashboard/generic-skeleton";
