// Ask renders a client panel with no heavy server await — the boundary barely shows.
// Neutral generic fallback; explicit so it doesn't inherit the overview-shaped root.
export { default } from "@/app/dashboard/generic-skeleton";
