// Product detail. Neutral generic fallback (matches prior root-inherited behaviour);
// explicit so it doesn't inherit the overview-shaped root loading.tsx.
export { default } from "@/app/dashboard/generic-skeleton";
