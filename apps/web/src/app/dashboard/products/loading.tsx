// Products index mostly redirects to a product's detail; the generic skeleton is a
// brief neutral fallback. Explicit so it doesn't inherit the overview-shaped root.
export { default } from "@/app/dashboard/generic-skeleton";
