// What's new renders a client view with no heavy server await — the boundary barely
// shows. Neutral generic fallback; explicit so it doesn't inherit the overview root.
export { default } from "@/app/dashboard/generic-skeleton";
