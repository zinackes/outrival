// Twitter card reuses the Open Graph image (same 1200×630). Next's file
// convention needs its own twitter-image route — re-exporting keeps one source.
export { default, alt, size, contentType } from "./opengraph-image";
