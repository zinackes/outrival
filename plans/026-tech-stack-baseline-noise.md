# Plan 026: Tech-stack signals fire only on real transitions, never on the baseline scan, and never at "high"

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**:
> `git diff --stat 6639163..HEAD -- apps/workers/src/jobs/scrape-tech-stack.job.ts apps/workers/src/lib packages/scrapers/src/tech-stack .env.example docs/architecture.md`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.
>
> **Base your branch on `origin/main` (`6639163`), NOT on `feat/shadcn-improve`.**

## Status

- **Priority**: P1
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: bug (signal reliability / alert fatigue)
- **Planned at**: commit `6639163` (origin/main), 2026-07-09

## Why this matters

The 2026-07-09 audit labeled 110 production signals
(`docs/audits/pipeline-audit-2026-07-09-golden-set.md`, validated): **31 of 110
(28 % of the entire feed) were tech-stack "New technology detected: Vercel / Netlify
/ Zendesk / HubSpot" signals, and effectively all of them were false adoptions** —
the *first* tech-stack scan of a competitor found the tech that was always there, and
the AI insight narrated it as "X has started using Vercel" (factually wrong). Four of
them shipped at severity **high** (HubSpot script detections) and landed in daily
digests. Root cause: the diff runs against an empty `tech_stack_entries` table on the
baseline run, so every detected tech is "appeared"; and importance→severity maps
`high → high`, treating a script/header tell as alert-tier intel. This plan removes
the single largest noise source in the product.

## Current state

All excerpts verified at `origin/main` = `6639163`.

- `apps/workers/src/jobs/scrape-tech-stack.job.ts` — the whole feature:
  - Line 31–33: severity mapping —
    ```ts
    function severityForImportance(importance: string): Classification["severity"] {
      return importance === "high" ? "high" : "medium";
    }
    ```
  - Lines 106–117: reconcile — `appeared = detected.filter((d) => !activeIds.has(d.techId))`
    where `activeIds` comes from the competitor's ACTIVE `techStackEntries` rows.
    **On the first-ever run this set is empty → everything "appeared".**
  - Lines 179–182: `competitors.techStackScrapedAt` is stamped every run —
    `techStackScrapedAt == null` before this update is therefore a reliable
    "this is the baseline scan" predicate (it is read into `competitor` at line 44,
    before the stamp).
  - Lines 184–195: signal gate —
    `const minImportance = process.env.TECH_STACK_SIGNAL_MIN_IMPORTANCE ?? "medium";`
    then `emitTechStackSignals(...)` for each appeared tech at/above it.
  - Lines 269–297 (`emitTechStackSignals`): per tech, a synthetic change with
    `diffText = "New technology detected on ${competitorName}: ${tech.name} (${tech.category}). Evidence: ..."`
    and a synthetic `Classification` (`category: "product"`, severity from the mapping,
    `reason: "${tech.name} (${tech.category}) newly detected in the competitor's stack"`)
    → `tasks.trigger("generate-signal", ...)`. The downstream insight model rewrites
    this as "has started using X" — the phrasing must make "detected ≠ adopted" explicit.
- `packages/scrapers/src/tech-stack/catalog.ts` — importance semantics (comment at
  lines ~40–47): `high` = business-meaningful tells (payments Stripe/Paddle, CRM…),
  `medium` = "worth a signal by default" (includes **hosting: Vercel line 219–230,
  Netlify 231–242, both `importance: "medium"`**, Mailchimp marketing 210…), `low` =
  never alerted. **Do not edit this file** — the fix is the job-side gate, not
  re-ranking the catalog.
- `.env.example` and `docs/architecture.md` both document
  `TECH_STACK_SIGNAL_MIN_IMPORTANCE=medium` as the default.
- Repo pattern for testable job logic: extract a pure module into
  `apps/workers/src/lib/` and test it from `apps/workers/test/*.test.ts` (bun) —
  exemplars: `apps/workers/src/lib/completeness.ts`, `apps/workers/test/jobs-delta.test.ts`,
  `apps/workers/test/rearm.test.ts`. Workers test command: `cd apps/workers && bun test test/`.

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Install | `pnpm install` | exit 0 |
| Typecheck | `pnpm typecheck` | exit 0 |
| Workers tests | `cd apps/workers && bun test test/` | all pass, 0 fail |

## Scope

**In scope** (the only files you should modify/create):
- `apps/workers/src/jobs/scrape-tech-stack.job.ts`
- `apps/workers/src/lib/tech-stack-signal.ts` (create — pure eligibility/severity helpers)
- `apps/workers/test/tech-stack-signal.test.ts` (create)
- `.env.example` (update the `TECH_STACK_SIGNAL_MIN_IMPORTANCE` default + comment)
- `docs/architecture.md` (the one line documenting that default)

**Out of scope** (do NOT touch):
- `packages/scrapers/src/tech-stack/catalog.ts` / `detector.ts` / `scraper.ts` —
  importance ranking stays as designed; the noise fix is the signal gate.
- `generate-signal.job.ts`, the insight prompts, the dispatcher — severity semantics
  elsewhere are plan 027's territory.
- `tech_stack_history` analytics writes and the `techStackEntries` upsert — the
  baseline run must STILL record entries + history (the profile tab depends on it);
  only the *signal* emission is gated.

## Git workflow

- Branch: `advisor/026-tech-stack-baseline-noise` off `origin/main`.
- Conventional commits, subject ≤ 50 chars, e.g. `fix(workers): no tech-stack signals on baseline scan`.
- Multi-line commit messages via `git commit -F <file>` (RTK proxy mangles multi-line `-m`).
- Do NOT push or open a PR unless the operator instructed it.

## Steps

### Step 1: Create the pure helper `apps/workers/src/lib/tech-stack-signal.ts`

```ts
import type { Classification } from "@outrival/ai";

const IMPORTANCE_RANK: Record<string, number> = { low: 1, medium: 2, high: 3 };

/** Severity for a tech-appearance signal. A script/header detection alone is never
 * alert-tier: high-importance tells (payments, CRM) cap at "medium", the rest at
 * "low". (Audit 2026-07-09: 4 of the recent "high" signals were marketing scripts.) */
export function severityForImportance(importance: string): Classification["severity"] {
  return importance === "high" ? "medium" : "low";
}

/** Which appeared techs deserve a signal. None on the baseline scan (first-ever
 * tech-stack run: everything "appears", none of it is news). */
export function signalEligibleTechs<T extends { importance: string }>(
  appeared: T[],
  opts: { isBaselineScan: boolean; minImportance: string },
): T[] {
  if (opts.isBaselineScan) return [];
  const min = IMPORTANCE_RANK[opts.minImportance] ?? 0;
  return appeared.filter((t) => (IMPORTANCE_RANK[t.importance] ?? 0) >= min);
}
```

(Exact shape may vary; keep both functions pure and exported.)

**Verify**: `pnpm typecheck` → exit 0.

### Step 2: Wire the job to the helper

In `apps/workers/src/jobs/scrape-tech-stack.job.ts`:

- Delete the local `IMPORTANCE_RANK` / `importanceRank` / `severityForImportance`
  (lines 23–33) and import from the new lib.
- Compute `const isBaselineScan = competitor.techStackScrapedAt == null;` — read it
  from the `competitor` row loaded at line 44 (i.e. BEFORE the stamp at lines 179–182).
- Replace the gate at lines 188–195 with:
  ```ts
  const minImportance = process.env.TECH_STACK_SIGNAL_MIN_IMPORTANCE ?? "high";
  const important = signalEligibleTechs(appeared, { isBaselineScan, minImportance });
  ```
  (note the default changes `"medium"` → `"high"`: hosting/marketing script tells —
  Vercel, Netlify, Mailchimp, Zendesk-class — stop signaling by default; payments/CRM
  still do, now at severity `medium`).
- When `isBaselineScan && appeared.length > 0`, log
  `logger.log("Baseline tech-stack scan — recording entries, no signals", { competitorId, appeared: appeared.length })`.
- Everything else (entry upsert, history rows, `techStackScrapedAt` stamp) unchanged.

**Verify**: `pnpm typecheck` → exit 0, and
`grep -n "signalEligibleTechs\|isBaselineScan" apps/workers/src/jobs/scrape-tech-stack.job.ts`
→ both present.

### Step 3: Make the synthetic change/classification wording honest

Still in `emitTechStackSignals` (lines 269–295):

- `diffText` becomes:
  `"Technology newly detected on ${competitorName} since the previous tech-stack scan: ${tech.name} (${tech.category}). Evidence: ${tech.evidence.join(", ")}. Note: detection reflects what the site exposes to visitors — it can lag or lead the actual adoption date."`
- `classification.reason` becomes:
  `` `${tech.name} (${tech.category}) newly detected since the previous tech-stack scan` ``

This text is the only grounding the downstream insight model gets — the explicit
"since the previous scan" + the note is what stops it writing "has started using X"
as a fresh adoption claim.

**Verify**: `grep -n "since the previous tech-stack scan" apps/workers/src/jobs/scrape-tech-stack.job.ts` → 2 matches.

### Step 4: Update the documented default

- `.env.example`: change the `TECH_STACK_SIGNAL_MIN_IMPORTANCE` line to `=high` and
  adjust its comment: "min tech importance to emit a signal on appearance (high =
  payments/CRM-class tells only; medium would include hosting/marketing scripts —
  noisy)".
- `docs/architecture.md`: update the single line documenting
  `TECH_STACK_SIGNAL_MIN_IMPORTANCE=medium` to `=high` with the same one-line
  rationale, and note that baseline scans never signal. Touch nothing else in the doc.

**Verify**: `grep -n "TECH_STACK_SIGNAL_MIN_IMPORTANCE" .env.example docs/architecture.md` → both show `high`.

### Step 5: Tests

`apps/workers/test/tech-stack-signal.test.ts` (bun test; model after
`apps/workers/test/jobs-delta.test.ts` for structure):

1. `signalEligibleTechs` returns `[]` when `isBaselineScan: true`, even with
   high-importance techs present.
2. Non-baseline + `minImportance: "high"`: filters out `importance: "medium"` (a
   `{ importance: "medium", name: "Vercel" }`-shaped item) and keeps `"high"`.
3. Non-baseline + `minImportance: "medium"`: keeps medium and high, drops low.
4. Unknown `minImportance` string (e.g. `""` from a mis-set env) does not throw and
   behaves as rank 0 (everything appeared passes) — assert current behavior explicitly
   so a future change is deliberate.
5. `severityForImportance("high")` → `"medium"`; `severityForImportance("medium")`
   → `"low"`; never `"high"` for any input.

**Verify**: `cd apps/workers && bun test test/tech-stack-signal.test.ts` → 5+ pass, 0 fail.

### Step 6: Full verification

**Verify**: `pnpm typecheck` → exit 0 · `cd apps/workers && bun test test/` → all pass.

## Test plan

Covered in step 5 — pure-function tests only; the job's DB flow is exercised in prod
and unchanged except for the gate.

## Done criteria

Machine-checkable. ALL must hold:

- [ ] `pnpm typecheck` exits 0
- [ ] `cd apps/workers && bun test test/` exits 0, incl. ≥ 5 new tests
- [ ] `grep -c "severityForImportance" apps/workers/src/jobs/scrape-tech-stack.job.ts` → import/usage only, no local definition
- [ ] `grep -n "isBaselineScan" apps/workers/src/jobs/scrape-tech-stack.job.ts` → present, derived from `techStackScrapedAt == null`
- [ ] `.env.example` + `docs/architecture.md` both say `TECH_STACK_SIGNAL_MIN_IMPORTANCE=high`
- [ ] No files outside the in-scope list modified (`git status`)
- [ ] `plans/README.md` status row updated

## STOP conditions

Stop and report back (do not improvise) if:

- `competitor.techStackScrapedAt` is no longer readable before the stamp (job
  restructured since `6639163`).
- `Classification` type import from `@outrival/ai` fails inside `apps/workers/src/lib/`
  (cross-package import restriction) — report; do not inline a duplicate type.
- Anything requires touching `catalog.ts` importance values to make the tests pass.

## Maintenance notes

- **Catalog-growth caveat**: when a new detector is ADDED to `TECH_CATALOG`, the next
  scan of every competitor reports that tech as "appeared" even though only our
  detection changed — the baseline gate does not cover this. If catalog changes
  become frequent, stamp a catalog version on `tech_stack_entries` and suppress
  appearances caused by version bumps (deferred deliberately).
- Ops behavior change to communicate: prod currently has
  `TECH_STACK_SIGNAL_MIN_IMPORTANCE` unset (default applies) — after deploy the
  default flips to `high` and hosting-class signals stop. That is the intent
  (audit: 31/110 feed items were this noise); anyone wanting the old behavior sets
  the env var to `medium` explicitly.
- Existing noise rows in prod (`signals` with source `tech_stack`) are NOT cleaned up
  by this plan — decide separately whether to bulk-dismiss them.
- Reviewer focus: confirm the baseline predicate uses the pre-stamp `competitor` row,
  not a re-fetched one.
