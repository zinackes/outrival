# Implementation Plans

Plans 001–004 generated by `improve` (mode `quick`) on 2026-07-07 @ `81c4b75`.
Plans 005–018 generated by `improve` (mode `deep`) on 2026-07-07 @ `81c4b75` (branch
`feat/shadcn-improve`). Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

Plans 021–024 = the **DX/reliability follow-on batch** (track A, chosen 2026-07-09 after the
001–020 backlog shipped). Planned @ `bf3a0ce`. Theme: finish the reliability baseline now that
CI (#126) exists — CI resilience (021), audit-in-CI (022), Biome lint/format (023), lockfile
refresh (024). 021 first (others build on the CI base); 023/024 land only when the PR queue is
clear (repo-wide diffs). **All TODO — not yet dispatched.**

Plans 025–030 = the **pipeline-reliability batch** from the 2026-07-09 audit
(`docs/audits/pipeline-audit-2026-07-09.md` + validated golden set
`docs/audits/pipeline-audit-2026-07-09-golden-set.md`). Planned @ **`6639163`
(origin/main)** — NOT this branch's HEAD: main moved past `81c4b75` (#123/#124/#125/
#126/#127/#128 merged), and 029/030 target #124-era pricing code. Executors MUST
branch from `origin/main`. All six are independent of each other; recommended order
= impact order: 025 (fake-success guard) → 026 (tech-stack noise, −28 % of the feed)
→ 027 (severity rubric + critical guard) → 028 (dead diff classes) → 029 (pricing
render floor) → 030 (staged-extraction cache). All worker-side changes need a manual
`trigger deploy` after merge.

Every plan is self-contained and stamped with the SHA it was written against; run the
plan's drift check first. Plans are independent unless "Depends on" says otherwise.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001  | `overtaken` AI-visibility signal only fires on real competitor gain | P1 | S | LOW | — | DONE — MERGED via #127 (`413a153`) ⚠️ needs `trigger deploy` |
| 002  | Stop `extract-*` jobs duplicating rows when the AI summary throws on retry | P1 | M | LOW | — | DONE — MERGED via #132 |
| 003  | Gate on-demand re-scans by plan (downgraded org can't refresh locked sources) | P2 | S | LOW | — | DONE — PR #137 (open) |
| 004  | `generate-signal` re-dispatches a signal committed but never dispatched | P2 | M | MED | — | DONE — PR #136 (open) |
| 005  | Every written test file actually executes (shared + scrapers) | P1 | S | LOW | 019 (scrapers half) | DONE — in PR #125 |
| 006  | CI gate (typecheck+test on PR) + non-cacheable `test` task | P1 | M | LOW | 005, 019, 020 | DONE — MERGED via #126 (`9f4afd3`) |
| 020  | Green the `apps/api` suite — env test missing the prod `TURNSTILE_SECRET_KEY` invariant | P1 | S | LOW | — | DONE — MERGED via #126 (bundled w/ 006) |
| 019  | Fix `mock.module` leak making the scrapers suite fail cross-file | P1 | S–M | LOW | — | DONE — in PR #125 (merged to main @ `1613188`) |
| 007  | Guard scraper fetch paths against SSRF (patch-32 sources + redirect re-validation) | P1 | M | MED | — | DONE — MERGED via #130 |
| 008  | Bump `hono` past the CORS origin-reflection advisory (≥4.12.25) | P1 | S | LOW | — | DONE — MERGED via #128 (`d13335f`) |
| 009  | Stripe subscription webhooks act on live state (no plan resurrection) | P1 | M | MED | — | DONE — MERGED via #131 |
| 010  | Failed forced re-scan reports an honest "failed" status (small migration) | P2 | S–M | LOW | — | DONE — PR #142 (open) ⚠️ migration 0031 to apply |
| 011  | Collapse the onboarding analysis poll storm into one self-terminating query | P2 | S–M | LOW–MED | — | DONE — PR #140 (open) |
| 012  | Stop shipping unused jsonb columns from the polled competitors-roster endpoint | P2 | S | LOW | — | DONE — PR #135 (open) |
| 013  | Make the Trends SSR prefetch key timezone-stable (stop discarding the seed) | P2 | S | LOW | — | DONE — PR #133 (open) |
| 014  | Reconcile agent-facing docs that describe removed/superseded systems as live | P2 | S–M | LOW | — | DONE — PR #141 (open) |
| 015  | Move the HMAC webhook signer into `@outrival/shared` (kill the mirror copy) | P3 | S | LOW | 005 | DONE — PR #138 (open) |
| 016  | Align the documented error-handling rule with the code (`Result<T,E>` mandate) | P3 | S | LOW | — | DONE — PR #139 (open) |
| 017  | Send an "all quiet" weekly digest on calm weeks (Lever 6) | P2 | S–M | LOW | — | DONE — PR #144 (open) |
| 018  | Resolve paid-but-undelivered Business features (`api`/`multiUser`) — deliver-or-hide | P2 | S–L | LOW–HIGH | — | DONE — PR #143 (open, operator chose HIDE) |
| 021  | CI resilience — pin Bun + bump deprecated Node-20 GitHub Actions | P2 | S | LOW | — | DONE — PR #145 (open, CI ✅ green; action-setup bumped v4→v6) |
| 022  | Gate CI on new reachable high/critical prod advisory (`pnpm audit --prod` + allowlist) | P1 | S–M | LOW | 021 (soft) | DONE — PR #146 (open, CI ✅ green; 17 GHSAs allowlisted) |
| 023  | Introduce Biome (format + conservative lint) + CI gate | P2 | M | LOW–MED | 021; land when queue clear | TODO |
| 024  | Deliberate in-range dependency refresh (bring the stale lockfile current) | P3 | M | MED | land when queue clear | TODO |
| 025  | Deny/soft-404/login pages can't become "success" snapshots or archive baselines | P1 | M | MED | — | DONE — MERGED via #155 (`d153e36`) ⚠️ needs `trigger deploy` |
| 026  | Tech-stack signals only on real transitions, never on baseline, never "high" | P1 | S | LOW | — | DONE — MERGED via #156 (`191ee28`) ⚠️ needs `trigger deploy` |
| 027  | Severity rubric in prompts + deterministic critical guard + retriable insight | P1 | M | MED | — | DONE — PR #157 (open) |
| 028  | Revive dead diff classes (section add/remove, price tweaks, og rebrand, recency) | P1 | M | MED | — | DONE — PR #159 (open) |
| 029  | Render retry for client-rendered pricing pages (priceless L0 captures) | P1 | S | LOW–MED | — | DONE — executed & reviewed (APPROVE), branch `advisor/029-pricing-render-floor` (`ae45d68`), awaiting user PR/merge ⚠️ needs `trigger deploy` |
| 030  | Staged-extraction heal/cache actually persists (normalize replay + cooldown stub) | P1 | M | LOW–MED | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

**029 — executed & reviewed 2026-07-10 (APPROVE), awaiting user PR/merge.** Re-anchored
from `6639163` to `origin/main` `0d35d0f` before dispatch (pricing PRs #151/#152/#158 +
plans 025–028 had landed; `pricing.scraper.ts` was byte-identical, `harvestPricing`'s
`{plans}` contract intact, and no render-retry logic existed yet — finding confirmed
still live). Commit `ae45d68` on `advisor/029-pricing-render-floor` (off `0d35d0f`,
disposable worktree). New pure trigger `render-retry.ts` (`needsRenderRetry(html, level)`
= L0 capture with zero harvestable prices) + `scrapeWithRenderRetry` helper wired into all
three single-page return points AND the homepage-embedded path (re-renders `resolvedBase`,
the post-redirect URL). Catalog-aggregation branch untouched; `harvest.ts`/`discover-url.ts`
consumed, not modified. Reviewer re-verified in the worktree: scope = exactly the 5 in-scope
files, `pnpm typecheck` 8/8, `packages/scrapers bun test src` **466 pass / 0 fail** (incl. 4
new render-retry tests exercising all branches: no-price SSR shell → true, priced → false,
level 1 → false, FR-format price → false). Not pushed. ⚠️ Worker-side scraper change → needs
`trigger deploy` after merge.

**001 — PR #127 (open) 2026-07-08.** Originally executed+APPROVE on `advisor/001-ai-visibility-overtaken`
(`6f7dad0`, off old main `81c4b75`). Since main moved to `9f4afd3` (incl. #123 organic-SoM which also
touched `diff.ts`), the approved commit was **cherry-picked clean onto `origin/main`** → branch
`fix/ai-visibility-overtaken` (`eb4ec44`) and **re-verified there**: workers typecheck exit 0, workers
suite **37 pass / 0 fail** (up from 33; #123 tests + 001's 2), fix line present, scope = 2 files.
Pushed + **PR #127** — **MERGED to main 2026-07-08** (`413a153`). The old `advisor/001-…`
branch/worktree is superseded. ⚠️ **PENDING: `trigger deploy` the workers** — worker code change
(the ai-visibility job) deploys manually from main, NOT via Coolify; the merge alone does not ship
the fix to the running job.

**008 — executed & reviewed 2026-07-08 (APPROVE) → PR #128 (open).** Commit `7eb251a` on
`advisor/008-bump-hono` (off `413a153`). First run STOPPED correctly: the plan's original
`pnpm update hono@^4.12.25` re-resolved the WHOLE workspace (~2600-line lockfile churn incl. the
drizzle/pglite peer + next/react/stripe/etc.) → scope guard tripped, reverted. **Plan 008 was
REVISED** to a conservative flow (hand-edit hono range → `pnpm install --lockfile-only`, never
`pnpm update`/`add`) — reviewer verified it stays minimal. Re-executed clean: hono `4.12.22→4.12.28`
+ an incidental benign `turbo 2.10.3→2.10.4`, NOTHING else. Reviewer re-verified on the commit:
scope = 2 files (hono+turbo only), `pnpm audit --prod` GHSA-88fw-hqm2-52qc **cleared** (0), typecheck
8/8, api tests 72/0. **First PR to exercise the new CI (#126).** Reusable: in this repo `pnpm update`
fans out — bump a single dep via edit + `--lockfile-only`. Not merged. Lockfile broadly stale → a
deliberate dep-refresh belongs in its own PR.

**005 + 019 — shipped together as PR #125** (`fix/test-suite-green` → `main`, OPEN/MERGEABLE,
2026-07-07): https://github.com/zinackes/outrival/pull/125. Rebuilt on current `origin/main`
(`54c389f`, which had advanced past `81c4b75` with #123/#124) and re-verified there: scrapers
`bun test src` **415 pass / 0 fail** (incl. #124's new pricing/ATS tests), shared **162 / 0**,
`turbo test` both packages exit 0, `pnpm typecheck` 8/8. 3 files, no product code. Awaiting user
review/merge. The per-plan review notes below are retained for the record.

**005 — executed & reviewed 2026-07-07 (unblocked — 019 now DONE; shipped in PR #125).** Executor's diff is correct and in
scope (both `package.json` edits). Reviewer re-verified: `packages/shared` `bun test src` =
**162 pass / 0 fail** (shared half done & landable now); `packages/scrapers` `bun test src` =
**381 pass / 2 fail** — the 2 failures are **pre-existing** (reproduce under the original
explicit command, `358 pass / 2 fail`) and unrelated to the plan's four target dirs
(`feeds/news/reddit/sitemap` = 23 pass / 0 fail). Root cause split out as **plan 019** (a
`mock.module` leak in `jobs-scraper.test.ts`). Key finding: **`pnpm test` is already red on
`main` (`81c4b75`)**, masked by turbo cache + no CI. Nothing merged; worktree diff preserved.

**019 — executed & reviewed 2026-07-07 (APPROVE), awaiting user merge.** Commit `5594447` on
branch `advisor/019-fix-scraper-test-isolation` (disposable worktree
`.claude/worktrees/agent-ac9676e45f6e8ca56`). One test file changed
(`jobs-scraper.test.ts`, +26/−5), no product code. Reviewer re-verified: `bun test src/jobs
src/lib` and reverse order both **123 pass / 0 fail**; `bun test src` (scrapers) **383 pass /
0 fail** (the 2 pre-existing failures gone, none reddened); `pnpm typecheck` exit 0.
Note: the plan's first two prescribed approaches (`afterAll` re-register; namespace-property
passthrough) both fail on Bun 1.3.13 quirks — the working fix captures the real
`scrapeFirstSuccess` as a plain `const` value **before** `mock.module`, then delegates to it
after an `afterAll` flag flip. Plan 019 on disk was corrected to this approach. Not merged/pushed.

**020 — executed & reviewed 2026-07-08 (APPROVE), awaiting user merge.** Commit `1e6db5f` on
branch `advisor/020-fix-api-env-turnstile-test` (off `origin/main` `1613188`, disposable worktree).
One file changed (`apps/api/test/env.test.ts`, +12/−0), `env.ts` untouched. Reviewer re-verified in
the worktree: `env.test.ts` **6 pass / 0 fail** (was 5/1-fail), full api suite **72 pass / 0 fail**
(10 files), `pnpm typecheck --filter @outrival/api` exit 0. The new negative case asserts the real
Turnstile prod invariant (not a no-op). Not merged/pushed.

**006 — executed & reviewed 2026-07-08 (BLOCKED, zero edits).** #125 (005+019) is now **merged
to `main`** (`origin/main` @ `1613188`), so 006 was dispatched against that green base (not the
stale `feat/shadcn-improve` HEAD, which lacks the fixes). The executor correctly made **no file
changes** and STOPped on the plan's pre-existing-failure STOP condition: **`pnpm test` is red on
`main`** — `apps/api#test` fails 1/71 at `test/env.test.ts` → `"production WITH both Upstash creds
parses"` expects `true`, gets `false`. Reviewer verified the root cause on `origin/main`:
`apps/api/src/env.ts` gained a second production `superRefine` (require `TURNSTILE_SECRET_KEY`,
by-design per `rules/production.md`) but the test's valid-config fixture was never updated to set
it. A real, deterministic failure on prod source — masked today by turbo cache + no CI, exactly
the class 006 exists to close. **Fix split out as plan 020** (test-only; do not relax `env.ts`).

**006 — RE-EXECUTED & reviewed 2026-07-08 (APPROVE), awaiting user merge.** After 020 landed,
006 was re-dispatched against `1e6db5f` (= `origin/main 1613188` + 020's env-test fix). This time
the full `pnpm test` was **green (12/12 tasks, api 72/0)** — so 020 was in fact the *only* remaining
red suite (the earlier "other suites may also be red" caveat is resolved). Commits `634c7a7` +
`9ad6bd1` on branch `advisor/006-ci-and-test-cache` (disposable worktree). 2 files: `turbo.json`
(`test` → `cache: false`, no `outputs`) + `.github/workflows/ci.yml` (checkout · pnpm 10.11.0 ·
node 20 · `oven-sh/setup-bun@v2` · frozen install → typecheck → test; triggers `pull_request` +
`push: main`). Reviewer re-verified in the worktree: scope = exactly those 2 files vs base; **double
`pnpm test` both bypass cache** (`cache bypass, force executing` on all 7 `#test` tasks, no `FULL
TURBO`) → the stale-replay hole is closed; final `pnpm test` exit 0. **Operator follow-up (outside
the repo files): make CI a required status check on `main` in branch-protection so a red build
blocks the Coolify auto-deploy.** **MERGED via PR #126** (squash `9f4afd3`) 2026-07-08 — CI is now
live on `main` (`ci.yml` present, `test` task `cache:false`). Branch-protection required-check is
the only remaining operator step (do it after CI has run once on the merge push).

**002 — executed & code-reviewed 2026-07-08 (APPROVE); push/PR PENDING.** Commit `9211de2` on
`advisor/002-extract-idempotency-reorder` (off `cd93ee8`, worktree `agent-aa9e6b4cc6e6d0ade`).
Reorders the 3 `extract-*` jobs so the throwing `summarizeSource`/`loggedAi` call runs BEFORE the
non-idempotent inserts (a retried run after an AI 429 can't leave duplicate reviews/pricing/counts).
Reviewer read all 3 diffs: behavior-preserving (same summarizeSource args, same insert columns incl.
#124's pricing additions + `salaryCurrency`, same guards `verbatims>0`/`!input.recordedAt`/`jobs>0||
closed>0`, `previousScore` still read pre-insert, jobs `countsByDept`/`closedTitles` hoisted from
already-available data — no use-before-def). Executor reported workers typecheck 0 + tests 44/0 (×2).
Reviewer re-verified after the transient classifier outage cleared: workers typecheck 0, tests
**44/0**, scope = 3 files, order confirmed (summarize precedes inserts in all three). → **PR #132
(open)**. Accepted residual (per plan): the jobs insert↔close window is untouched (separate finding).

**009 — executed & reviewed 2026-07-08 (APPROVE) → PR #131 (open).** Commit on
`advisor/009-stripe-webhook-ordering` (off `cd93ee8`). Webhook `updated`/`created` now re-retrieve
live sub state (`getStripe().subscriptions.retrieve`) instead of the event snapshot; `deleted`
no-ops when the deleted sub ≠ the org's current one. 2 files: `stripe-webhook.ts` (+19/−3) + new
`apps/api/test/stripe-webhook.test.ts` (5 cases). **Plan was refined pre-dispatch**: the original said
create the test under `src/routes/` — the api suite globs `test/`, so it'd have silently never run
(the 005 orphan class); also added concrete Stripe-stub guidance (harness doesn't mock Stripe).
Reviewer re-verified on the commit: typecheck 8/8, api `bun test test/` **77/0**, the 5 tests pass in
suite AND isolation (`--timeout 60000`; default-5s isolation "fail" was a PGlite cold-init timeout, not
fragility). Audited the 2 regression tests — case 3 (event-active + retrieve-canceled → stays free)
and case 4 (superseded delete → unchanged, guard log observed) both genuinely fail on the old code.
Dunning `past_due→paid` pinned. Not merged.

**007 — executed & reviewed 2026-07-08 (APPROVE, 1 revision round) → PR #130 (open).** Commits
`537f23b`/`4134e04`/`104413a`/`9574bb0` on `advisor/007-scraper-ssrf-guard` (off `cd93ee8` — the
executor twice STOPPED correctly on the base guard as `main` advanced under it via #128/#129, neither
touching scrapers). New `safeFetch` helper (per-hop `validatePublicUrl` + manual redirect) routes 9
raw-`fetch` sites (changelog/sitemap/status + scrape-direct/conditional-fetch/discovery/tech-stack/
alternatives/discover-url); `wayback.ts` (fixed host) + `quick-fetch.ts` (reference) untouched.
Round-1 STOP was legit — the last file broke an out-of-scope test whose `mockFetch` omitted `headers`;
reviewer authorized completing that mock (a real Response always has one). Executor also flagged a
2nd identical incomplete mock (`commerce-candidates.test.ts`) — same one-line faithful fix, no
assertions touched (approved on merit). Reviewer re-verified on the commit: 13 files, both test edits
are `+ headers:{get:()=>null}` ONLY, typecheck 8/8, scrapers `bun test src` **420/0** (incl. the new
`guarded-fetch.test.ts`: internal-host & redirect-to-internal both throw, the latter with exactly 1
fetch call), grep `redirect:"follow"` → only wayback + a quick-fetch comment. Not merged.

**005 — READY once 019 lands.** Its two `package.json` edits are correct (reviewed 2026-07-07,
worktree `.claude/worktrees/agent-aac35ed7926f9fd23`, uncommitted). The only thing that kept it
from green was the scrapers suite bug now fixed by 019. **Merge order for a green `pnpm test`:
019 + 005 together** (005's shared half is green alone; 005's scrapers-glob half needs 019's fix
present). After both land, re-run 005's done criteria — expect `pnpm test` exit 0.

**025 — executed & reviewed 2026-07-09 (APPROVE, 2 revision rounds); push/PR PENDING.** Commit
`3f39bce` on `advisor/025-deny-page-guard` (off `bae6d01`), worktree
`/home/tmfzi/outrival/.claude/worktrees/agent-a2f142c443db7b18a`. Reviewer re-verified in the
worktree: `pnpm typecheck --force` exit 0 (8/8), scrapers **435 pass / 0 fail** (15 new deny-page
tests), workers **61 pass / 0 fail**, scope = the 7 in-scope files. Two false-positive classes the
plan had NOT anticipated were caught in review, not by the plan's own criteria — both would have
silenced a monitor permanently (`partial` on every capture ⇒ `skipDiffForPartial` + no extraction,
visible only in a `logger.warn`):
1. **Ungated branches.** `<input type="password">` and the verification-copy regex ran without the
   `< 3000 visible chars` gate. Repro: a long real homepage with a hidden `<dialog>` login form →
   `login_wall`; a docs article quoting "One moment, please" → `verification_wall`. Fixed: all four
   kinds are now length-gated.
2. **Synthetic documents.** The detector ran on the docs *our own scrapers build* from parsed data.
   Repro: a small real sitemap listing `/404` → `soft_404`. Fixed: `isSyntheticDocument()` (marker
   `data-outrival-{sitemap,news,reddit,changelog,ats,line}`, deliberately excluding `billing` — that
   marks an appended block on a real page) + `SYNTHETIC_DOC_SOURCES` for the marker-less always-built
   sources (`sitemap`/`news`/`reddit`/`github_repo`) + `!monitor.apiCaptureEnabled`.

Approved deviation: the plan named two consumers of `completeness`; there are **three**. The executor
also rewired `extractionAllowed` (`scrape-monitor.job.ts:~1219`) — without it a deny page graded
`partial` still fed `extract-pricing`/`extract-jobs`/`extract-reviews`, contrary to the plan's goal.
⚠️ **PENDING: workers deploy.** This is worker code — it ships via `trigger deploy` from `main`, not
Coolify. Before merge, the plan's own maintenance note still applies: eyeball the deny regexes
against a handful of real tracked competitor pages.

**026 — executed & reviewed 2026-07-09 (APPROVE, 0 revision rounds) → MERGED via #156 (`191ee28`).**
Commit `2138a17` on `advisor/026-tech-stack-baseline-noise` (off `origin/main` `d153e36`), worktree
`/home/tmfzi/outrival/.claude/worktrees/agent-ad0c64e2f63cc4527`. Reviewer re-verified in the
worktree: `pnpm typecheck --force` exit 0 (8/8), workers **74 pass / 0 fail** (7 new pure tests),
scope = the 5 in-scope files, `catalog.ts` untouched. Baseline predicate reads the in-memory
`competitor` row loaded once at line 33, before the `techStackScrapedAt` stamp at line 174 — the DB
write doesn't mutate the local object, so it is correctly pre-stamp.

⚠️ **Ops, before deploy.** Prod leaves `TECH_STACK_SIGNAL_MIN_IMPORTANCE` unset, so the default flips
`medium → high` on deploy: hosting/marketing-script tells (Vercel, Netlify, Zendesk, HubSpot) stop
signalling entirely, payments/CRM tells still fire but now at severity `medium`, never `high`. That is
the intent (31/110 of the sampled feed was this noise). Anyone wanting the old behaviour sets the var
to `medium` explicitly. **Latent footgun (pre-existing, now pinned by a test):** the read is
`process.env.X ?? "high"`, and `??` does not catch the empty string — a var *set but empty* (easy to do
in Coolify) yields rank 0 and every appeared tech signals, including `low`. Leave it unset, don't blank it.

Not covered, deliberately: the existing tech-stack noise rows already in prod `signals` (bulk-dismiss is
a separate decision), and the catalog-growth case — adding a detector to `TECH_CATALOG` makes that tech
"appear" for every competitor on the next scan, which the baseline gate does not catch.

⚠️ **PENDING: workers deploy** — worker code, ships via `trigger deploy` from `main`, not Coolify.

**027 — executed & reviewed 2026-07-09 (APPROVE, 0 revisions); push/PR PENDING.** Commit `c980c13` on
`advisor/027-severity-rubric-critical-guard` (off `d153e36`), worktree
`/home/tmfzi/outrival/.claude/worktrees/agent-a1ff3a8a9f2277c55`. Reviewer re-verified: `pnpm typecheck
--force` 8/8 exit 0, workers **75 pass / 0 fail** (8 new guard tests), scope = 5 files,
`notification-dispatcher.ts` untouched. Rubric sits inside the static `CLASSIFY_SYSTEM` const, so the
free provider-side prefix cache still hits.

Approved deviation: rather than the plan's `finalSeverity` + 6 renames, the executor made `severity` a
`let` and reassigned it once, immediately after the guard, before any downstream read. Verified — the
only read between declaration and reassignment is the value passed *into* the guard. Smaller diff, same
invariant.

Reviewer checks worth keeping: (a) the retriable throw is safe — the first DB write in the job is the
signal insert at line 332, long after the throw at 286, so a retry replays only reads, the AI calls and
append-only `ai_runs` rows; (b) on the structured-homepage path `changes.diff_text` is
`renderStructuredChanges(...)`, so the guard's `PRICE_TOKEN` check has real text to match — a pricing
critical whose rendering carries no currency token demotes to `high`, the intended conservative
direction ("if unsure, choose high").

Watch after deploy: severity distribution shifts by design (first genuine criticals appear, `medium`
share drops), and classifications are cached 7 days keyed on input — for up to a week, previously-seen
diffs replay pre-rubric labels. Do not bust the cache. If a false critical slips both rubric and guard,
tighten the allowlists in `severity-guard.ts` before re-wording the prompt. Worker + AI code:
`trigger deploy` from `main`.

**028 — executed & reviewed 2026-07-09 (APPROVE, 0 revisions) → PR #159 (open).** Four independent
fixes, one commit each (`a45ac29` A, `03f572c` B, `31b668a` C, `e61cfc7` D) on
`advisor/028-revive-dead-diff-classes` (off `d153e36`), worktree
`/home/tmfzi/outrival/.claude/worktrees/agent-a4dc645d039cb9d51`. Plan was **re-anchored** from
`6639163` to `d153e36` before dispatch: PR #155 shifted `scrape-monitor.job.ts` by ~50 lines (recency
query 1016→1066, significance call 1161→1210); `diff/`, `scoring/`, `significance.ts` were byte-identical.

Reviewer re-verified in the worktree: `pnpm typecheck --force` 8/8, scrapers **451 pass / 0 fail**,
ai **66 pass / 0 fail**, scope = 7 in-scope files, four clean per-fix commits.
- **Fix A** correctness hinges on the invariant `sectionHistory[0]=current, [1]=diff's prev`. Confirmed
  at the call site (`scrape-monitor.job.ts:949` reads the last 6 snapshots `desc(scrapedAt)` incl. the
  one just inserted). Polarity confirmed: `filterUnstableSections` KEEPS a `section_added` when
  `added.has(heading)` is true, so the `ConfirmAllSet` (has()→true) correctly makes a no-prior-history
  capture a pass-through. The rewritten tests drive real `diffHomepages` output, not hand-built sets —
  the genuine-add regression case actually exercises the bug that was impossible since #74.
- **Fix D** the `{sourceType}` param is optional, so the second caller (`backfill-history.job.ts`)
  keeps compiling untouched; the timestamps-only negative test confirms a pricing source without a
  price token is still rejected.

Judgment call (executor, accepted): dropped an old `section-stability` test case that encoded the same
impossible index-1 state as the one the plan explicitly calls out; implemented exactly the plan's 6
prescribed cases, no 7th.

⚠️ **Expect new signal volume after deploy** (the point): section adds/removes resume (dead since
2026-07-04), busy competitors' homepage changes stop being damped by a blog elsewhere, og rebrands and
bare price changes now reach classification. If noise returns, the lever is the stability `window` (3)
and per-field weights — not re-breaking the filter. Worker + scrapers + AI code: `trigger deploy`.

Note on the stacked index: 025–028 all edit `plans/README.md`; the appended notes will conflict on
sequential merge to main — resolve by keeping both. Rows themselves are already at final state here.

## Recommended sequencing

The deep-audit set (005–018) is leverage-ordered above. Suggested flow:

1. **Reliability baseline first** — **005/019 (done, #125 merged) → 020 → 006**. 005 made the
   19 orphaned test files run and 019 fixed the scrapers mock leak (both merged to `main`).
   Executing 006 then revealed `pnpm test` is *still* red on `main` via a stale `apps/api` env
   test → **020** greens it; **006** then wires CI and stops `turbo test` from replaying a stale
   pass. Landing these first makes every test-backed fix below trustworthy.
2. **Security** — **007** (SSRF regression on worker fetch paths) and **008** (hono CORS
   advisory, one-command bump). Independent; do both early.
3. **Money** — **009** (Stripe webhook out-of-order/duplicate → plan resurrection). Its test
   step is more trustworthy after 005/006.
4. **Correctness/perf quick wins** — **010, 012, 013** are small and low-risk; **011** is a
   slightly larger polling refactor (do after 012, since both touch the roster payload).
5. **Docs/debt** — **014, 015, 016** any time (low risk); 015 wants 005 landed so the shared
   test script exists.
6. **Direction** — **017** (all-quiet digest, validated feature) and **018** (decide
   deliver-or-hide for the paid-but-undelivered features; default to the reversible HIDE).

## Dependency notes

- **019 unblocks 005 & 006**: the scrapers suite is red on `main` today (a `mock.module` leak,
  not the orphaned dirs). 005's scrapers half can't reach exit-0 and 006 shouldn't gate CI until
  019 lands. Sequence for the reliability baseline is now **019 → 005 (scrapers half) → 006**;
  005's **shared half is already green and can land independently** of 019.
- **006 depends on 005 + 020**: wiring CI to a suite that only half-runs (005) — or that is
  outright red (020, the `apps/api` env-test drift found while executing 006 on 2026-07-08) —
  defeats the purpose. 005/019 are merged (#125 @ `1613188`); 020 + 006 executed & APPROVED
  2026-07-08 (worktree branches, awaiting user merge) — the reliability baseline is complete.
- **020 blocks 006, depends on nothing**: test-only fix to `apps/api/test/env.test.ts` so the
  production `TURNSTILE_SECRET_KEY` invariant (already enforced, by-design, in `env.ts`) is
  reflected in the test fixtures. Do NOT relax `env.ts` to green it.
- **015 depends on 005**: the shared-package webhook test needs `@outrival/shared` to have a
  `test` script (added by 005). Without 005, run its test file directly.
- **007 pairs with 005** (not a hard dep): landing 005 first means the new `guarded-fetch`
  test auto-enrolls under the broadened scrapers glob.
- **009's test quality depends on 005/006** (not a hard dep): the webhook tests run either
  way via `apps/api`'s own suite, but CI (006) makes them a real gate.
- 001–004 remain independent of 005–018.

## Findings considered and rejected (so they aren't re-audited)

- **Transitive `undici`/`ws`/`systeminformation` high advisories** — real advisories, but
  the security pass judged them **not on reachable exploit paths** (undici via cheerio
  parsing not fetching; ws/systeminformation via Trigger host-metrics with no user input).
  Not worth a plan now; revisit if a deeper reachability audit changes the verdict. (Only the
  **direct** hono CORS advisory is planned — 008.)
- **`next-mdx-remote@5` RCE advisory** — real, but MDX is repo-authored (read from disk), so
  not attacker-reachable today. Noted as a low-urgency bump in plan 008's Maintenance, not a
  standalone plan.
- **JSON-LD `dangerouslySetInnerHTML` escaping** — the data feeding the three JSON-LD sinks is
  static/repo-authored today (not attacker-reachable). Defense-in-depth only; fold the escape
  into any future change that wires dynamic/scraped/AI content into those components.
- **`signal-batching` non-transacted insert+update** and **digest send-before-stamp double
  email** — real but narrow (require a mid-loop crash in a small window); lower leverage than
  the planned set. Worth a small transaction/idempotency-key follow-up if they ever recur.
- **ai-visibility missing `AT TIME ZONE 'UTC'`** — real convention violation, low visible
  magnitude (only the "checked <date>" line / day bucketing for cross-day-boundary viewers).
  Fold into any future edit of `ai-visibility.ts`.
- **`lib/api.ts` god-module (2,976 lines, fan-in 116)** and **finishing the TanStack Query
  migration (17 self-fetchers)** — real, high-value tech-debt, but **L effort** decomposition
  work better scheduled as a deliberate refactor project than an advisor handoff plan. Not
  rejected on merit — deferred by size. Same for the other >1000-line god files
  (`competitor-detail-view.tsx`, `competitors.ts`, `scrape-monitor.job.ts`) and the shadcn
  primitive normalization (signals-view hand-rolled sheet).
- **No linter/formatter (Biome)** — worth doing (M effort + an initial cleanup pass), but a
  formatting sweep is best run interactively by the operator (ratchet from non-blocking) than
  as an executor plan; add it as a CI step once 006 lands.
- **`.env.example` drift, agent-hook 127KB injection, root README, radix/turbo pinning,
  notification-insert dedup** — genuine DX/debt nits, individually below the planning
  threshold; batch them into a housekeeping PR when convenient.

## What was audited (deep, 2026-07-07)

Eight parallel read-only passes across the whole monorepo — correctness, security,
performance, test coverage, tech-debt/architecture, dependencies, DX/tooling+docs, and product
direction — with every table finding re-verified by opening the cited code. Verified **healthy**
(stated so it isn't re-audited): tenant isolation on API hotspot routes, parameterized SQL
(Drizzle), the `decideDispatch` 5-layer dispatcher, the diff engines (homepage/section-stability/
pHash/volatile/relevance), R2-key construction (server-generated), and the already-current
dependency stack (Next 16 / React 19 / Tailwind 4 / Stripe 22 / drizzle 0.45 / better-auth 1.6).
**Not turned into plans by size/scheduling** (see rejected list): the L-effort god-file
decompositions, the TanStack migration tail, and the lint/format introduction.

## What was audited (pipeline reliability, 2026-07-09 — plans 025–030)

Targeted audit of the scrape→diff→classify→aggregate→alert pipeline against the
landing-page promises, on ~6 weeks of production data (read-only prod SELECTs) plus
six parallel code audits. Full report: `docs/audits/pipeline-audit-2026-07-09.md`;
110-signal labeled golden set (operator-validated): `docs/audits/pipeline-audit-2026-07-09-golden-set.md`.
**Correction to the 2026-07-07 "verified healthy" list above**: the section-stability
filter is NOT healthy — its confirmation window contradicts the diff's emission
precondition, making `section_added/removed` impossible since #74 (fixed by plan 028).

Audit findings deliberately NOT planned in this batch (so they aren't re-audited):

- **Ops check, 5 minutes, no code**: prod has ZERO L2+ scrape runs in 30 days, all 785
  `monitors.requires_level` NULL, 84 unresolved Cloudflare challenges → verify
  `PROXYSCRAPE_*` env vars exist/work on the prod worker before anything else.
- **Landing copy fixes (marketing decision, not executor work)**: "100% EU / data
  never leaves the EU" is contradicted by the site's own /subprocessors page (R2
  `outsideEea: true`, US AI providers) — legal exposure, fix the copy or the
  infrastructure; "Scanned hourly" (real cadence: realtime=2h, daily=25h); "15+
  source types" (13 max selectable; linkedin/twitter have no scraper); "high …
  within 5 minutes" (high routes to the daily digest by design).
- **`dispatched_channel` stamped before the plan gate** (`email_immediate` recorded
  for Free orgs where nothing sends) — corrupts any future latency/delivery metric;
  small fix, fold into the instrumentation batch below.
- **Instrumentation batch** (deferred as a group — needs product sign-off on the
  metrics): critical-latency SLO in ops-health-check; `changes_detected/changes_silenced`
  counters on scrape_runs; `items_extracted/input_signal` on extraction_runs;
  time-to-first-signal admin tile (data already collected, never read);
  `alerts.error` + digests generated-vs-sent monitoring; `ai_runs.model` recording
  the ACTUAL model (markModel twin of markProvider).
- **Aggregation (AGG-1/2/3)**: batching is write-only (UI render disabled by #77,
  digests don't read batches) and its competitor+category key can't merge the
  cross-source "pricing overhaul" scenario (prod: 0 batches ever). Needs a product
  decision first — repair (re-enable batch cards + drop category from the key +
  digest merging) or remove the feature and the landing claim. Don't plan until decided.
- **Diff false-silence siblings deferred by design complexity**: DIF-5 (pure visual
  redesign never triggers pHash — hash-identical early return), DIF-6 (annual-only
  price changes invisible — toggle block stripped from hash), DIF-8 (A/B hero
  flip-flop learning), DIF-2 (testimonial exact-window).
- **Scraper family gaps below this batch's cut**: blog/changelog L0-only escalation
  (SCR-7/9, 53 dead blog monitors), blog RSS-first (SCR-8), SmartRecruiters
  pagination + Workable/BambooHR/WTTJ connectors (SCR-11/12), review verbatims
  pagination + JSON-LD Review nodes (SCR-13), Statuspage incidents.json (SCR-16),
  sitemap 5000-cap mid-walk truncation (SCR-17), news homonym brands (SCR-19),
  timeout-never-escalates tarpit trap (CAS-2), requiresLevel 14-day ratchet cost
  (CAS-5), kill-switch naming off-by-one (CAS-6).
- **Classifier org-relative severity** (CLS-7: same diff, same severity for every
  org regardless of overlap) — needs a cache-key/product design decision;
  revisit after 027's rubric lands and the golden set is re-scored.
